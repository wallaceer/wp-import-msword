on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: 📂 Sync files
      uses: wallaceer/wp-import-word@v1.1
      with:
        server: 77.83.64.38
        username: fb24.com_jice99s6cve
        password: ${{ secrets.ftp_password }}
        server-dir: /httpdocs/wp-content/uploads/
        exclude: |
          **/.git*
          **/.git*/**
        
